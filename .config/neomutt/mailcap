text/plain; $EDITOR %s ;
text/html; firefox '%s' &; test=test -n "$DISPLAY"; needsterminal;
text/html; lynx -dump %s; nametemplate=%s.html; copiousoutput;
image/*; sxiv %s ;
video/*; mpv %s ;
audio/*; mpv %s ;
application/pdf; firefox %s ;
application/pgp-encrypted; gpg -d '%s'; copiusoutput;
application/pgp-keys; gpg --import '%s'; copiusoutput;
